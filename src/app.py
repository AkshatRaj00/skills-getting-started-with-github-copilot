Implemented the missing FastAPI endpoints in **src/app.py**: added an in‑memory `activities` dictionary, a `GET /activities` route to list all activities, and a `POST /activities/{name}/signup` route that validates the email, checks capacity, prevents duplicate sign‑ups, and returns appropriate HTTP responses. This completes the exercise’s required functionality.